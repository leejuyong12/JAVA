



```

String 비교

.equals
혹은
==

.equals는 값을 비교한다.
== 는 주소값을 비교한다.

즉. 값에 대한 정확한 비교를 위해서는 equals를 사용해야한다.
```



```
char 과 String 비교

char과 String을 비교하기 위해서는 == 를 사용해야하는데 
String에서 ""(쌍따옴표)가 아닌 ''(홑따옴표)를 사용해야 한다.

char c = inputP.charAt(i);
if(c == 'a'){
	~~~
}

```

